# Core dependencies (un-pinned here so CI/runners can pick compatible builds)
# For exact reproducibility, use a pinned requirements lock file locally.

pandas
numpy
scipy
scikit-learn
statsmodels
matplotlib
# econml is optional and heavy; see requirements-optional.txt
